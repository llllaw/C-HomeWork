#include<iostream>
#include<stdio.h>
#include<string.h>
#include<string>
using namespace std;
int main(){
	/*
	string a;
	getline(cin, a);
	int result = 0;
	for (int i = a.size()-1; i >= 0; i--){
		if (a[i] != ' '){
			result++;
		}
		else{
			cout << result;
			return 0;
		}
	}
	*/
	char a[1000000];
	while (scanf_s("%s", a));//这种方式会一直进行下去
}
